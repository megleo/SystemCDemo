{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile",
            "type": "shell",
            "command": "cd ${workspaceRoot}/build && cmake .. && make -j8",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
